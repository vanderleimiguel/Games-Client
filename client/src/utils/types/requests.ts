export type LoginRequest = {
  Email: string;
  Password: string;
};

export type IdActual = {
  IdActual: string;
};

export type UserInput = {
  Name: String;
  Email: String;
  Password: String;
  CPF: String;
};

export type User = {
  Id: String;
  Name: String;
  Email: String;
  Password: String;
  CPF: String;
};
